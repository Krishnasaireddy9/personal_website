import {useRef} from 'react';import {motion,useMotionValue,useSpring} from 'framer-motion'
export default function MagneticButton({children,className='',...props}){const ref=useRef(null),x=useSpring(useMotionValue(0),{stiffness:180,damping:14}),y=useSpring(useMotionValue(0),{stiffness:180,damping:14});return <motion.a ref={ref} style={{x,y}} onMouseMove={e=>{const r=ref.current.getBoundingClientRect();x.set((e.clientX-r.left-r.width/2)*.22);y.set((e.clientY-r.top-r.height/2)*.22)}} onMouseLeave={()=>{x.set(0);y.set(0)}} className={className} data-interactive {...props}>{children}</motion.a>}
