import {Code2,BrainCircuit,Network,Container,Database,ShieldCheck} from 'lucide-react';import {motion} from 'framer-motion';import Reveal from './Reveal';import {skillGroups} from '../data/skills'
const icons={Code2,BrainCircuit,Network,Container,Database,ShieldCheck}
export default function Skills(){return <section id="skills" className="section"><Reveal><p className="eyebrow">02 / Technical toolkit</p><div className="mb-12 flex flex-col justify-between gap-4 md:flex-row md:items-end"><h2 className="title">One stack.<br/><span className="text-violet-400">Endless leverage.</span></h2><p className="max-w-md text-white/45">From orchestration graphs and neural networks to APIs, databases, and the tests that keep them honest.</p></div></Reveal><div className="grid auto-rows-auto grid-cols-1 gap-4 md:grid-cols-7">{skillGroups.map((g,i)=>{const Icon=icons[g.icon];return <motion.article key={g.title} className={`glass group relative overflow-hidden rounded-3xl p-6 ${g.size} ${g.featured?'bg-violet-500/[.09] shadow-glow':''}`} initial={{opacity:0,y:30}} whileInView={{opacity:1,y:0}} viewport={{once:true}} transition={{delay:i*.06}} whileHover={{y:-6,borderColor:'rgba(167,139,250,.45)'}}><div className="absolute -right-10 -top-10 h-32 w-32 rounded-full bg-violet-500/0 blur-3xl transition group-hover:bg-violet-500/20"/><Icon className="mb-10 text-violet-400" size={g.featured?38:30}/><h3 className="mb-4 font-display text-xl font-semibold">{g.title}</h3><div className="flex flex-wrap gap-2">{g.items.map(x=><span className="pill" key={x}>{x}</span>)}</div></motion.article>})}</div></section>}
